---
description: Show the BugDex — the bug species catalogued in this repo.
argument-hint: "[--type <type>] [--status <status>]"
---

Run `node "${CLAUDE_PLUGIN_ROOT}/bin/bugdex.cjs" dex $ARGUMENTS` and show me the catalogue.

If the list is long, highlight the notable entries: any **Nemeses** (recurring, unsealed) and any **legendary** species, and point out types with no catches yet.
