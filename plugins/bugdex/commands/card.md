---
description: Show my BugDex trainer card — rank, XP, streak, and active Nemeses.
---

Run `node "${CLAUDE_PLUGIN_ROOT}/bin/bugdex.cjs" card` and show me my trainer card.

If there are active Nemeses, remind me I can convert them into permanent wins with `/bugdex:seal <id>`.
