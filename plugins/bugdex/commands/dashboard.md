---
description: Open the BugDex Pokédex web dashboard.
argument-hint: "[--port <port>]"
---

Run `node "${CLAUDE_PLUGIN_ROOT}/bin/bugdex.cjs" dashboard $ARGUMENTS` to serve the Pokédex web UI, then share the local URL with me.

> The dashboard lands in milestone M6.
