/**
 * BugDex dashboard server — serves the Pokédex web UI (a single inlined HTML
 * build) plus a tiny JSON API over `.bugdex/` (SPEC §8).
 */
export * from "./state.js";
export * from "./api.js";
export * from "./server.js";
